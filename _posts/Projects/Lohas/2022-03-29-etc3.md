---
layout: single
title: "react-datepicker"

categories:
  - Lohas
---

## React-Datepicker

시공예정날짜 등 날짜를 입력해야할 상황이 오게되었다!! 찾다가 가장 많이 사용하는 라이브러리인 React-Datepicker를 찾게되었다.

### 설치

```jsx
npm install react=datepicker --save
```

### 커스텀

공식 사이트에 많은 커스텀 예시들이 있었고 이것을 복붙하면 바로 사용할 수 있었다!!  
하지만 나는 깔끔하게 사용하고 싶어서 기본 커스텀에 몇가지만 변경하였다.

```jsx
import React, { useState } from "react";
import DatePicker from "react-datepicker";
import "react-datepicker/dist/react-datepicker.css";

const Calender = () => {
  const [startDate, setStartDate] = useState("");

  return (
    <StyledBody>
      <DatePicker
        dateFormat="yyyy-MM-dd"
        selected={startDate}
        onChange={(date) => setStartDate(date)}
        placeholderText="날짜를 입력해주세요"
      />
    </StyledBody>
  );
};
```

dateFormat을 이용해 날짜 포맷을 변경할 수 있었다!!
